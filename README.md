## project-G

project-G

> **project-G** – учебный проект, демонстрирующий работу с самописным плоским
> файлом-“диском” и консольной утилитой-клиентом. Он позволяет создавать,
> удалять, читать и обновлять файлы в одиночном контейнере `disk.filesystem`
> с чрезвычайно простым текстовым форматом.

---

## Участники проекта:
- Нигматзянов Амир
- Хасанов Камиль

---

## Структура репозитория

| Файл/папка         | Назначение                                                        |
|--------------------|-------------------------------------------------------------------|
| `README.md`        | Документация проекта                                              |
| `presentation.pptx`| Презентация файловой системы                                      |
| `logo.png    `     | Логотип файловой системы                                          |
| `main.c  `         | Основной файл с точкой входа                                      |
| `filesystem.c`     | Реализация функций файловой системы                               |
| `disk.filesystem  `| Файл виртуального диска                                           |

---

## Формат файла-диска

```
/absolute/path/to/file1.txt
Первая строка содержимого файла 1
Вторая строка содержимого файла 1
/another/file
весь следующий текст относится к файлу /another/file
/third
...
```

--- 

## Функции работы с файловой системой

### create_new_file
Описание: Создает новый файл в файловой системе
Параметры:
- `FILE* fs` - указатель на файловый поток
- `const char* filename` - имя создаваемого файла

const char* content - содержимое файла
**Возвращаемое значение:**
int - 0 при успехе, -1 при ошибке

**Пример использования:**
```c
if (create_new_file(fs, "new.txt", "Пример содержимого") == 0) {
    printf("Файл успешно создан\n");
}
```

### `modify_file_content`
**Описание:** Изменяет содержимое существующего файла
**Параметры:**
- `FILE* fs` - указатель на файловый поток
- `const char* filename` - имя изменяемого файла
- `const char* new_content` - новое содержимое файла
**Возвращаемое значение:**
- `int` - 0 при успехе, -1 если файл не найден

  **Пример использования:**

```c
if (modify_file_content(fs, "test.txt", "Новое содержимое") == 0) {
    printf("Файл успешно изменен\n");
}

```
